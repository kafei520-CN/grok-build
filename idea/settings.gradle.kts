pluginManagement {
    repositories {
        maven("https://www.jetbrains.com/intellij-repository/releases")
        maven("https://www.jetbrains.com/intellij-repository/snapshots")
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "grok-build-idea"
